services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "127.0.0.1:5432:5432"
    environment:
      POSTGRES_USER: quant
      POSTGRES_PASSWORD: quant_dev_2026
      POSTGRES_DB: quant
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: