# 审查记录

## 2026-08-14 文档第一轮

- 路径：独立 Codex reviewer subagent；审查者未修改文件。
- 主要发现：不能在无回测时声称提高胜率；缠论确认规则与防未来函数不够精确；缺少行情调用预算、120 分钟会话对齐、交易日历、候选池生命周期、通知投递语义和 Webhook 合同。
- 处理：全部采纳并修订需求、设计、实施计划和测试计划。MVP 默认采用沪深 A 股、排除 ST/退市、分时等同当日 1 分钟线/VWAP、至少一次投递加幂等键；复权口径在最终代码审查后改为默认 `none`，以保证腾讯日线与分钟线一致。
- 未纳入本期：策略收益回测。没有成交/退出标签定义时只交付筛选排序，明确不承诺提高真实胜率。

## 2026-08-14 文档第二轮

- 路径：同一独立 Codex reviewer subagent 复审；审查者未修改文件。
- 主要发现：卖点缺少独立评分路径；VWAP 数据合同缺成交额；全市场 500 只轮转会漏信号；复权能力和包含方向规则不够可执行。
- 处理：增加买卖双评分和卖点 UAT；Bar 增加 amount；改为可续跑历史回填后批量日增量；禁止复权口径混用；修正包含方向为非包含 K 线高低点同向变化。

## 2026-08-14 最终代码审查

- 路径：独立 Codex reviewer subagent；审查者未修改文件。
- 高风险发现：最后一笔可变导致缠论信号重绘；腾讯原始分钟线被误标 `qfq`；停牌旧数据可能作为新信号；全市场游标失败仍会标记完成；企业微信 HTTP 200 应用错误被误判成功；outbox 过期 worker 可反写新 worker 状态。
- 处理：信号只使用被下一笔锁定的 confirmed strokes；默认改为 `none` 且复权能力不满足时拒绝周期；事件和日线扫描校验最近应有交易日；回填按成功集合计算覆盖率；校验企业微信 `errcode`；outbox 增加 claim token 条件更新。
- 修复复核：审查者发现股票池变化时旧成功代码未与当前股票池取交集，可能虚增覆盖率；已在计算剩余股票前执行集合交集，并增加股票新增/退市变化回归测试。

## 2026-08-14 接管后的最终复核

- 路径：新的独立 Codex reviewer subagent；审查者未修改文件。
- 高风险发现：持久化回填成功状态未重新验证本地历史；股票列表未稳定排除上市不足 120 日股票；新浪停牌零值快照可能被写成当天已收盘日线。
- 中风险发现：`adjust: none` 在 Tushare 适配中被字符串真值误判；outbox 一次领取 100 条而租约只有 5 分钟，后半批可能被另一 worker 重领。
- 处理：成功集合每轮按历史存在性、根数和最新交易日重新校验；东方财富/Tushare 列表按上市日期过滤，缺少上市日期的降级源按实际历史长度当日暂缓并后续复查；拒绝零价和非法 OHLC 快照并使用最近应有交易日落盘；修正 Tushare `none` 判断；outbox 改为逐条 claim/发送。新增对应回归测试。
- 修复复核：同一独立审查者确认上述高/中风险项均已关闭，未发现新的阻断项。残余风险是无上市日期的降级源无法绝对区分真实新股与持续截断的成熟股票历史，以及供应商若用非零昨收价配合零成交表示停牌时只能依赖后续数据/信号新鲜度控制。
- 非本任务改动：`openspec/config.yaml` 在本任务开始前已经处于删除状态，接管过程中未恢复或改写。
